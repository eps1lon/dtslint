{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "watch": "tsc --watch",
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build",
    "create-rule": "create-dtslint-rule",
    "lint": "lerna run lint -- --silent",
    "test": "lerna run test"
  },
  "devDependencies": {
    "@types/fs-extra": "^5.0.4",
    "@types/node": "^7.0.68",
    "@types/strip-json-comments": "0.0.30",
    "create-dtslint-rule": "file:packages/create-dtslint-rule",
    "lerna": "^3.0.0-rc.0",
    "tslint": "^5.9.1",
    "typescript": "~2.9.2"
  }
}
