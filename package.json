{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "watch": "tsc --watch",
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build",
    "lint": "lerna run lint -- --silent",
    "test": "lerna run test"
  },
  "devDependencies": {
    "@types/node": "^7.0.68",
    "lerna": "^3.0.0-rc.0",
    "tslint": "^5.9.1",
    "typescript": "~2.9.2"
  }
}
